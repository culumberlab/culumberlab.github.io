document.querySelectorAll('.nav-toggle').forEach(button=>button.addEventListener('click',()=>{const nav=document.querySelector('.site-nav');const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open));}));

document.querySelectorAll('[data-research-viewer]').forEach(viewer=>{const image=viewer.querySelector('.viewer-image');const link=viewer.querySelector('.viewer-link');const title=viewer.querySelector('.viewer-title');const description=viewer.querySelector('.viewer-description');const tabs=viewer.querySelectorAll('.viewer-tab');tabs.forEach(tab=>tab.addEventListener('click',()=>{image.src=tab.dataset.src;image.alt=tab.dataset.alt;image.classList.toggle('cover',tab.dataset.fit==='cover');link.href=tab.dataset.src;title.textContent=tab.dataset.title;description.textContent=tab.dataset.caption;tabs.forEach(item=>{const selected=item===tab;item.classList.toggle('active',selected);item.setAttribute('aria-selected',String(selected));});}));});

